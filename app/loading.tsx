import PageSkeleton from '@/components/system/PageSkeleton';

export default function RootLoading() {
  return <PageSkeleton title="Cargando Fiambrería Costanera POS" subtitle="Iniciando la aplicación y tu sesión más reciente." rows={5} />;
}
